
#include <iostream>
using namespace std;

int main()
{
    int user_num;
    int quotient;
    int remainder;
    int prev;
    int binary;

    while (quotient != 0) {
        prev = user_num;
        quotient = user_num / 2;
        remainder = user_num / 2;
        prev = quotient;
        //binary (concatenate with remainder)
    }

    cout << binary;
}
